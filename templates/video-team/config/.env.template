TIER1_MODEL=ollama/qwen3:32b
TIER2_MODEL=ollama/qwen3-coder:30b
OLLAMA_BASE_URL=http://localhost:11434
HUMAN_EMAIL=your-email@example.com
HUMAN_PHONE_NUMBER=+15551234567
OUTLOOK_ADDRESS=mike.felkey@outlook.com
OUTLOOK_PASSWORD=your-outlook-password
GITHUB_TOKEN=your-github-pat-here
GITHUB_USERNAME=mfelkey
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ── Video generation APIs ─────────────────────────────────────────────────────
# These are populated based on which tools the Tool Intelligence Analyst recommends.
# Only keys for tools you actually have access to need values.
VEO2_PROJECT_ID=                        # Google Cloud project ID for Vertex AI / Veo 2
VEO2_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=        # Path to GCP service account JSON
RUNWAY_API_KEY=                         # Runway Gen-4 API key
KLING_API_KEY=                          # Kling API key
PIKA_API_KEY=                           # Pika API key (fallback)
LUMA_API_KEY=                           # Luma Dream Machine API key (fallback)

# ── Avatar APIs ───────────────────────────────────────────────────────────────
HEYGEN_API_KEY=                         # HeyGen v2 API key
SYNTHESIA_API_KEY=                      # Synthesia API key (fallback)
DID_API_KEY=                            # D-ID API key (fallback)

# ── Audio / TTS APIs ──────────────────────────────────────────────────────────
ELEVENLABS_API_KEY=                     # ElevenLabs TTS (primary voiceover)
SUNO_API_KEY=                           # Suno music generation
UDIO_API_KEY=                           # Udio music generation (fallback)
STABLE_AUDIO_API_KEY=                   # Stability AI audio (fallback)

# ── Image generation (for AI still frames) ───────────────────────────────────
REPLICATE_API_KEY=                      # Replicate (Flux, SDXL via Runway i2v)

# ── Tool cache ────────────────────────────────────────────────────────────────
# Path where the Tool Intelligence Analyst saves its last recommendation
# for delta comparison on the next job.
VIDEO_TOOL_CACHE_PATH=memory/tool_cache.json
